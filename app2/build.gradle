dependencies {
    implementation project(":app")
}

tasks.withType(ScalaCompile) {
    scalaCompileOptions.forkOptions.jvmArgs = [
        "-Xms501m",
        "-Xmx501m",
        "-XX:+AlwaysPreTouch"
    ]
}